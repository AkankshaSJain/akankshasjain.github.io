import React from "react";
import "./../App.css";

function Projects() {
	return (
		<div className="projectSection" id="projects">
			<h2 className="subComponentHeaders"> Projects </h2>
			<p> <b>Search Application (Spring MVC, JSP, Servlets, Rest API)</b>
			<br/>
			RESTful web application which takes input string as a parameter and searches the string and related patterns in a large repository of data and gives the reference to those files. Also it saves the frequency of the searched words and stores the results in cache for faster access.
			</p>
			<p> <b>Search Application (Spring MVC, JSP, Servlets, Rest API)</b>
			<br/>
			RESTful web application which takes input string as a parameter and searches the string and related patterns in a large repository of data and gives the reference to those files. Also it saves the frequency of the searched words and stores the results in cache for faster access.
			</p>
			<p> <b>Search Application (Spring MVC, JSP, Servlets, Rest API)</b>
			<br/>
			RESTful web application which takes input string as a parameter and searches the string and related patterns in a large repository of data and gives the reference to those files. Also it saves the frequency of the searched words and stores the results in cache for faster access.
			</p>
		</div>
	)
}

export default Projects;